main() {
  /// arthematic opertators
  /// +, - , *, / , %

  /// c= a+b
  // Operand = a,b,c,1,2
  // Operator: + , =

  int sum = 1 + 33 * 22 - 11;

  /// comparison operators
  /// == , <, >, <= , >=  // true, false

  bool eqauls = 1 >= 1;

  /// Logical operators
  /// && , ||, !

  bool result = 1 == 2 && 1 < 5;

  bool isSunday = !true;

  // unary operatos
  // --, ++

  int count = 0;
  count++;
  ++count;

  count--;
  --count;

  count = count + 1;
  count += 1;

  // ternary operator

  isSunday == true ? print("today is sunday") : print("not sunday");

  // null related operators: ?., ?? ,
}
